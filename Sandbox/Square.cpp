#include "Square.h"

void Square::render(const scene::Scene& parent, const std::vector<GameObject*>* const objects, const std::vector<scene::Light*>* const lights) const
{/*
	Shape(SQUARE);
	Color(color);
	Mode(draw);
	Texture(t);
	Animation(a);
	Render();
	Mode(fill);
	Render();

	*/

}
